# Server Configuration
PORT=3000

# Max file size in MB (default: 200)
MAX_FILE_SIZE=200

# Cache time in seconds (default: 86400 = 24 hours)
CACHE_TIME=86400

# Security: Allowed domains (comma-separated, leave empty to allow all)
# Examples:
# ALLOWED_DOMAINS=example.com,docs.google.com,drive.google.com
# ALLOWED_DOMAINS=mycompany.com (allows mycompany.com and *.mycompany.com)
ALLOWED_DOMAINS=

# Security: Accepted file formats (comma-separated)
# Default: .pdf,.doc,.docx,.xls,.xlsx,.ppt,.pptx,.csv,.odt,.ods,.odp
ACCEPTED_FORMATS=.pdf,.doc,.docx,.xls,.xlsx,.ppt,.pptx,.csv,.odt,.ods,.odp
